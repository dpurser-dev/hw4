<h1>Places</h1>

<ul>
  <% if @places %>
    <% for place in @places %>
      <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
    <% end %>
  <% else %>
    <li>Not logged in</li>
  <% end if %>
</ul>

<p>
  <a href="/places/new">New place</a>
</p>